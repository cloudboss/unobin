stack: {
  factory: {
    inputs: {
      base-path: '/tmp/unobin-for-each'
      files:     { alpha: 'first message', beta: 'second message' }
    }
  }

  state: local {
    path: '.unobin/state'
  }

  encryption: noop {}
}
