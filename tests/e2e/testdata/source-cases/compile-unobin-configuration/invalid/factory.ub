factory: {
  inputs: { name: { type: string } }
  imports: { aws: 'github.com/example/cloudlib' }
  library-configs: {
    aws: { regoin: 'us-east-1' }
  }
  resources: { this: aws.thing { name: input.name } }
}
