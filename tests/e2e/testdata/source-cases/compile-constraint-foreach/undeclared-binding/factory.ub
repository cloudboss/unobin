factory: {
  inputs: {
    rules: { type: optional(list(string)) }
  }
  constraints: [
    {
      kind: predicate
      @for-each: [ { @rule: var.rules } ]
      when:    true
      require: @x.value != null
    },
  ]
}
