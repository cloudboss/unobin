factory: {
  inputs: {
    replicas: { type: optional(list(object({ tls: optional(boolean) }))) }
  }
  constraints: [
    { kind: predicate, @for-each: input.replicas ?? [], when: true, require: @each.value.nope == true },
  ]
}
