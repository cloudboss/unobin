factory: {
  inputs: {
    rules: { type: optional(list(string)) }
  }
  constraints: [
    {
      kind: predicate
      @for-each: [
        { @a: @b.value.items },
        { @b: input.rules },
      ]
      when:    true
      require: true
    },
  ]
}
