factory: {
  inputs: {
    rules: { type: optional(list(string)) }
  }
  constraints: [
    {
      kind: predicate
      @for-each: [ { @rule: input.rules } ]
      when:    true
      require: @each.value != null
    },
  ]
}
