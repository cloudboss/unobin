factory: {
  inputs: {
    code: { type: optional(object({ inline: optional(string) })) }
    size: { type: optional(integer) }
  }

  constraints: [
    { kind: predicate, when: input.code.inline != null, require: input.size != null },
  ]
}
