inputs:
  greeting: string  default: 'hello'  (sensitive)  -- Text to write
  count: integer
  enabled: boolean
  tags: list(string)
outputs:
  endpoint  (sensitive)  -- Public endpoint URL
  token  (sensitive)
  plain
