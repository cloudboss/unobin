# Expression positions in examples/ whose inferred type still
# contains Unknown, one per line as <file>:<line>:<col> <type>.
# Empty is the goal: an addition is a regression to explain, a
# removal is progress to record here.
