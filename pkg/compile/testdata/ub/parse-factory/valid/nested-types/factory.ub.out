factory: {
  imports: {
    core: 'example.com/core'
  }

  inputs: {
    cfg: { type: object({ port: { type: integer, default: 8080 } }) }
  }

  resources: {
    app: core.thing {
      port: input.cfg.port
    }
  }
}
