inputs: {
  subnet-set: { type: integer, default: 0 }
}

resources: {
  subnet-a: core.thing { name: 'subnet-a', size: 1 }
  broken: core.pending-list {
    name: 'broken'
    subnet-ids: if input.subnet-set
      then [resource.subnet-a.id]
      else []
  }
}
