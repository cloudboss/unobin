data-sources: { many: core.lookup { @for-each: input.keys, key: @each.value } }
outputs: {
  alpha-value: { value: data-source.many['alpha'].value }
  beta-value:  { value: data-source.many['beta'].value }
}
