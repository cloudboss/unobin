factory: {
  assets: {
    archive: './archive.zip'
    tree: './tree'
  }

  imports: {
    native: 'github.com/example/native'
  }

  locals: {
    result: native.inspect(asset.archive.content)
  }
}
