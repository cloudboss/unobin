locals: {
  aws-config: {
    assume-role: {
      role-arn:    'arn:aws:iam::123456789012:role/unobin-state'
      external-id: 'unobin-test'
    }
  }
}

factory: {
  configurations: {
    aws.default: local.aws-config
    aws.east:    { assume-role: local.aws-config.assume-role }
  }
}
