guarded-field: input.tls?.port
guarded-chain: input.cfg?.db?.host
guard-after-plain: resource.aws.vpc.main.logging?.bucket
guard-then-index: input.cfg?.hosts[0]
guard-call-argument: core.format('%s', input.cfg?.db)
