inputs: {
  replicas:  { type: list(object({ host: optional(string) })) }
  listeners: { type: list(object({ cert: optional(string) })) }
}
constraints: [
  {
    kind: required-together
    fields: [input.replicas[*].host, input.listeners[0].cert, input.volumes[*].id]
  },
]
